[package]
name = "ohosky_api"
version = "0.1.0"
edition = "2024"

[lib]
name = "ohosky_api"
crate-type = ["rlib", "staticlib"]

[dependencies]
anyhow = { version = "1.0.100", features = ["backtrace"] }
async-ffi = { version = "0.5.0", features = ["macros"], optional = true }
bytes = { version = "1.11.0", features = ["serde"] }
cxx = { version = "1.0.192", features = ["c++20"], optional = true }
obfstr = "0.4.4"
rkyv = "0.8.14"
safer-ffi = { version = "0.1.13", default-features = false, features = [
    "async-fn",
    "nightly",
    "serde",
    "tokio",
], optional = true }
serde = { version = "1.0.228", features = ["derive"] }
secrecy = "0.10.3"
tokio = { version = "1.49.0", features = [
    "sync",
    "macros",
    "io-util",
    "rt",
    "time",
] }
tracing = "0.1.44"
wit-bindgen = { version = "0.51.0", optional = true }
zerocopy = "0.8.33"

[build-dependencies]
cxx-build = "1.0.192"

[features]
default = []
cxx = ["dep:cxx"]
ohosky = ["wit-bindgen"]
skydream = ["async-ffi", "safer-ffi"]
